#include <iostream>  // Подключение библиотеки для ввода/вывода
#include <cstring>   // Подключение библиотеки для работы со строками в стиле C
#include "FlightManager.h"  // Подключение заголовочного файла менеджера рейсов
#include "TextProcessor.h"  // Подключение заголовочного файла обработчика текста

// Главная функция программы - точка входа
int main() {
    // Вывод заголовка программы
    std::cout << "=== Лабораторная работа №2 ===" << std::endl;
    std::cout << "=== Система управления рейсами Аэрофлота ===" << std::endl;
    
    // Создание объекта менеджера рейсов
    // При этом автоматически вызывается конструктор FlightManager()
    FlightManager manager;
    
    // Запуск основного меню программы
    // Этот метод содержит весь основной цикл работы с пользователем
    manager.runMenu();
    
    // Возврат 0 означает успешное завершение программы
    // Когда функция main завершается, автоматически вызываются деструкторы всех объектов
    return 0;
}
